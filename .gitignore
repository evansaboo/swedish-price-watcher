node_modules
.env
data/*.json
data/*.db
data/*.db-shm
data/*.db-wal
data/*.migrated
!data/.gitkeep
.env*.local
# Timestamped backups written by scripts/setup-discord-buy.sh — these hold secrets.
.env.bak.*
version.json
.DS_Store

# Scratch verification scripts run against the live host.
*.tmp.mjs
